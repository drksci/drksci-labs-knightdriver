import { LitElement } from 'lit-element';
import { ElementPin } from './pin';
export declare class MembraneKeypadElement extends LitElement {
    /**
     * Number of columns (3 or 4)
     */
    columns: '3' | '4';
    /**
     * Whether to display a connector beneath the keypad
     */
    connector: boolean;
    /**
     * Key labels
     */
    keys: string[];
    get pinInfo(): ElementPin[];
    private pressedKeys;
    renderKey(row: number, column: number): import("lit-element").SVGTemplateResult;
    render(): import("lit-element").TemplateResult;
    private keyIndex;
    private down;
    private up;
    private keyStrokeDown;
    private keyStrokeUp;
}
