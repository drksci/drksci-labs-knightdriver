import { LitElement } from 'lit-element';
import { ElementPin } from './pin';
/** The potentiometer SVG is taken from https://freesvg.org/potentiometer and some of the
    functions are taken from https://github.com/vitaliy-bobrov/js-rocks knob component */
export declare class PotentiometerElement extends LitElement {
    min: number;
    max: number;
    value: number;
    step: number;
    startDegree: number;
    endDegree: number;
    private center;
    private pressed;
    readonly pinInfo: ElementPin[];
    static get styles(): import("lit-element").CSSResult;
    mapToMinMax(value: number, min: number, max: number): number;
    percentFromMinMax(value: number, min: number, max: number): number;
    render(): import("lit-element").TemplateResult;
    private focusInput;
    private onValueChange;
    private down;
    private move;
    private up;
    private updatePotentiometerPosition;
    private rotateHandler;
    private updateValue;
}
