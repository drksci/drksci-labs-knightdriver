import { LitElement } from 'lit-element';
import { ElementPin } from './pin';
export declare class LCD1602Element extends LitElement {
    color: string;
    background: string;
    characters: number[] | Uint8Array;
    font: Uint8Array;
    cursor: boolean;
    blink: boolean;
    cursorX: number;
    cursorY: number;
    backlight: boolean;
    pins: 'full' | 'i2c' | 'none';
    protected numCols: number;
    protected numRows: number;
    get text(): string;
    set text(value: string);
    static get styles(): import("lit-element").CSSResult;
    protected get panelHeight(): number;
    get pinInfo(): ElementPin[];
    get cols(): number;
    get rows(): number;
    path(characters: Uint8Array | number[]): string;
    renderCursor(): import("lit-element").SVGTemplateResult[] | null;
    renderI2CPins(): import("lit-element").SVGTemplateResult;
    renderPins(panelHeight: number): import("lit-element").SVGTemplateResult;
    render(): import("lit-element").TemplateResult;
}
